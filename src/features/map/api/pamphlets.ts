/**
 * 지도에서 쓰는 팜플렛 조회. 구현은 features/mypage/api/pamphlets.ts 한 곳에 있다 —
 * 마이페이지와 지도가 같은 API를 부르므로 두 벌로 두지 않는다.
 */
export { fetchPamphletDetail, fetchPamphlets } from '@/features/mypage/api/pamphlets'
